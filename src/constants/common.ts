export const PATH = {
  PUBLIC: 'public',
  PRIVATE: 'private',
};

export const ROUTES = {
  BASE: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  DASHBOARD: '/dashboard',
  CHAT: '/chat/:id',
};

export const LIMIT_MESSAGES = 20;
